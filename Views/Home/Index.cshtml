<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tubbataha Explorer Marine Species Directory</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/VisualStudio.styles.css" asp-append-version="true" />
    <style>
        /* Reset margins and paddings */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;
            overflow: hidden; /* Prevent scrolling */
        }

        /* Style for the image background */
        .image-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1; /* Place image behind other content */
            background: url('/css/bg/bg3.jpg') no-repeat center center/cover; /* Set the background image */
        }

        /* Style for the content container */
        .content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            text-align: center;
            color: white;
            font-family: Arial, sans-serif;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7); /* Add shadow for better visibility */
            padding: 1rem;
        }

        /* Title Styles */
        .title-large {
            font-size: 4rem;
            font-weight: bold;
            margin: 0.5rem 0;
        }

        .title-small {
            font-size: 2rem;
            font-weight: normal;
            margin: 0.5rem 0;
        }

        /* Description Styles */
        .description {
            font-size: 1.1rem;
            margin: 1rem auto;
            width: 90%; /* Adjust width for responsiveness */
            max-width: 600px; /* Limit max width for larger screens */
        }

        /* Ensure navbar is above the image */
        .navbar {
            z-index: 1000;
        }

       
    </style>
</head>
<body>
    <!-- Image Background -->
    <div class="image-background"></div>

    <!-- Content Container -->
    <div class="content">
        <h1 class="title-large text-center">Tubbataha Explorer</h1>
        <h2 class="title-small">Marine Species Directory</h2>
        <p class="description d-none d-md-block">
            Welcome to the administrative dashboard for the Tubbataha Explorer Marine Species Directory. As an administrator, you have the tools to manage system settings and review user-submitted files. Approve or reject uploads to ensure they contribute meaningfully to marine conservation and the protection of Tubbataha's rich biodiversity.
        </p>
    </div>
</body>
</html>
